{
  "name": "audio-editor-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers for Audio Editor API",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}